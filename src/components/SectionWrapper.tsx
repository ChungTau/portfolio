import { styled } from "@mui/material";

export const SectionWrapper = styled('section')({
    position: 'relative',
    zIndex: 10,
    maxWidth: '1150px',
    margin: 'auto',
    padding: '4.6rem',
    overflow: 'hidden'
});